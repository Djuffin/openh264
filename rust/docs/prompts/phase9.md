# Phase 9 — the safety endgame (charter)

*Written 2026-08-22 by the steward at `acebd103` (Phase 8b closed). This is the map the
phase executes against; individual sessions get their own briefs. Every number here was
grepped against the tree at that commit — re-grep before quoting (S24).*

## 1. What Phase 9 is

Phases 5 and 6 gave the decoder and encoder **owned** data — `Vec`, `Option<Box>`,
arrays, handles — in place of raw allocations. Phase 5 then made the decoder's **access**
to that data safe (borrows, slices, `PicId`/`MbGrid` handles) and the decoder now carries
`#![deny(unsafe_code)]` on all 22 modules with essentially zero `unsafe-cat` tags. Phase 9
does for the **encoder** what Phase 5 did for the decoder: retire the raw cursors the
encoder still hands around, so its `unsafe` disappears rather than being tagged.

The encoder is **already** under `#![deny(unsafe_code)]` on all 39 files. It compiles only
because every remaining unsafe site carries a tagged `#[allow(unsafe_code)]`. The tags are
the work queue. At `acebd103`:

| tag | count | what it is | owner |
|---|---|---|---|
| `port-raw(Phase 9)` | **695** | raw-pointer signatures and unsafe bodies below the boundary | **this phase** |
| `cursor` | **61** | owned-storage cursor accessors (S28/S40 tests) + the 22 dispatch survivors | **this phase** |
| `C-ABI` | 45 | the frozen FFI boundary | stays (Phase 8) |
| `MT` | 21 | the fork/join seam's neighbourhood | **this phase** (retires with the ctx split) |
| `SCREEN_CONTENT(dormant)` | 8 | the FME search half | Phase 10 |
| `C-ABI(test)` | 5 | the Miri driver and P13's probes | stays |
| `send-seam(Phase 9)` | 1 | D-mt-1's one `unsafe impl Send` | **this phase** |
| `recon-seam` | **2** *(added by session C, `85a525d3`; unchanged by C2, which added consumers and no new raw)* | D-mt-3's reconstruction seam: one `UnsafeCell`-crossing accessor and one `unsafe impl Sync` in `encoder/rec_view.rs` | **lawful** — retires only if D-mt-3 is revisited |

Definition of done (plan section 2.3, unchanged): every module outside `src/api/` is
unsafe-free and raw-pointer-free; `*mut`/`*const` survive only in the `#[repr(C)]` ABI
types and the boundary thunks; `libc` is gone; the crate carries `#![deny(unsafe_code)]` at
the root with the only `#[allow]` on `src/api/`.

## 2. The shape of the 695 (measured, not assumed)

> **Corrected by session A (F101, T9.A1).** The table below as first written was an
> artefact: it was produced by reading only the **first line** of each tagged item, and
> the port wraps signatures past ~100 columns, so every multi-line signature looked as
> if it had no raw parameter and was filed as "body-only, converts for free". The
> authority is now `rust/docs/phase9_census.md`, regenerated by
> `python3 rust/tools/phase9_census.py --write`. **Body-only is 147, not 419.**

Classified by the raw-pointer types in the whole parameter list of the item each tag
sits above. `pure` = every raw parameter is of this family, so the family's own session
can convert the site unaided; `total` also counts sites whose *latest* family is this
one but which carry an earlier family too.

| family | pure | total | *(first-line artefact)* | what it is | retires by |
|---|---:|---:|---:|---|---|
| **body-only `unsafe fn`** | — | **147** | *419* | signature carries no raw pointer; unsafe because the body derefs a cursor or calls an unsafe fn | falls out as its callees go safe — **not converted directly** |
| **`*mut sWelsEncCtx` param** | **111** | **234** | *120* | the F66 conversion. 111 are ctx-only (F66's 109 convertible + two `*mut *mut` out-params); 123 carry a cursor as well | after the cursors (F66) |
| **other raw sig** | **80** | **123** | *31* | **not one family** — bitstream, parameter sets, RC, LTR, VAA, deblock, trace, scalar out-params. **Unowned in §8** | needs its own session split |
| **`*mut u8` plane/byte** | **57** | **64** | *51* | plane cursors and prediction buffers | the plane conversion |
| **layer/pic/slice ptr** | **47** | **55** | *33* | `*mut SDqLayer`/`SPicture`/`SSlice` | the picture/layer conversion (F73) |
| **SMbCache/SMB** | **45** | **48** | *15* | `*mut SMbCache`/`*mut SMB` per-macroblock metadata | the MB-metadata conversion |
| **coeff cursor** | **19** | **19** | *25* | `*mut i16`/`*const i16` DCT/quant/scan buffers | **5 done (T9.A3/A4)**; the other 14 are `SWelsFuncPtrList` slots whose call-throughs pass SMbCache cursors, so they retire with the MB-metadata conversion (F103) |
| **dispatch** | **4** | **5** | *—* | `SWelsFuncPtrList` slot and fn-pointer signatures | de-virtualization |

**`pure` is a property of the signature, not of the call sites** (F103). A kernel can
have a single-family signature and still be unconvertible because of what its callers
pass; the coefficient family lost 15 of 19 that way. Read the call sites before scoping.

The decisive fact (F66, phase6_findings): converting a `*mut sWelsEncCtx` parameter to
`&mut sWelsEncCtx` is **blocked until the raw cursors are gone**. A `&mut ctx`
function-entry retag invalidates the *whole* 65-field context under Stacked Borrows, so any
caller holding a context-derived cursor across the call is UB (93 such sites in 28 callers;
session J made the conversion, Miri refused it, it was reverted). The port's whole idiom is
"derive a cursor from ctx, call something, use the cursor" — sound only while the cursor is
**raw** (a raw retag pops only the offsets it touches). So the order is forced:

> **cursors first (planes, coeff, MbCache, layer/pic) -> then the `*mut ctx` conversion ->
> then the 147 body-only fns go safe as their callees do.**

The kernels the cursors feed are **already safe**. `mc.rs`'s `copy_4x4`,
`encode_mb_aux.rs`'s `dct_4x4`/`quant_4x4`, `sad_common.rs`'s SADs all take
`&PlaneCursor`/`&mut [i16; N]` today; the `unsafe` is the **shim** that wraps them for a
raw-pointer caller (`copy_shim(pDst: *mut u8, ...)` -> `from_raw_parts`). A family converts
by making its **callers** hold the safe type; the shim then has no raw caller and is
deleted, and the shared `common/` kernel loses its last raw entry point. This is why
`common/`'s 9 un-denied files (mc, sad_common, deblocking_common, intra_pred_common,
copy_mb, expand_pic, wels_common_defs, wels_trace, cpu_core) are **not an independent
task** — they go `deny` as the encoder callers that reach their shims convert.

## 3. The safe vocabulary already exists

Do not invent types. Phase 5 built and proved these; the encoder reuses them:
- `safe::plane::{PaddedPlane, PlaneCursor, PlaneCursorMut}` — plane roots and bounded
  cursors (S37/S40 retag-stable roots).
- `safe::pool::{Pool, Id}` — owned slots addressed by handle (grow/shrink added in 8b.C).
- `safe::mb_grid::MbGrid` — single-owner per-MB metadata (`forbid(unsafe_code)`).
- The encoder's picture pool (`encoder::picture`) and layer storage already own their data
  (Phase 6); what is raw is the *access*.

## 4. The families, in dependency order (the session plan)

Each family is a Phase-5-style rollout: convert the callers to hold the safe type, delete
the shims, retire the tags, Miri-verify. **Sizes below are session A's census
(`phase9_census.md`), not the estimates this section first carried.**

1. **Coefficient cursors** (19 signatures) ✅ **CLOSED** — a closed residual pipeline
   (DCT -> Hadamard -> quant -> scan -> NZC) whose kernels are already safe. **Session
   A's proof family**, and it split (F103): the **5** with a direct caller converted in
   T9.A3/A4; the other **14** are `SWelsFuncPtrList` slots whose 59 call-throughs pass
   SMbCache walking cursors, so they moved to family 3 — and **landed there** in
   T9.D8/T9.D10. The `PQuant*`/`PScan*` typedefs go with them, not here.
2. **Plane cursors** (57 pure / 64 total `*mut u8`, + F73's picture-accessor family) and with
   them `common/`'s mc/sad/deblocking_common/intra_pred_common shims. The largest; the
   reconstruction and source planes reached through the pool.

   > **Re-scoped by session B's caller census (F104 — `phase9_plane_census.md`).** The
   > split this section describes — "source/reference are read-only, so session B converts
   > those plus the owned-buffer operands" — is right about the *surfaces* and wrong about
   > what that makes convertible. Every cost-table call site is safe on its plane operands,
   > and **25 of the 37 pair that operand with an `SMbCache` prediction buffer**; a table
   > has one type, so `pfSampleSad`/`pfSampleSatd`/`pfSample4Sad` flip only after *both*
   > the plane roots and family 3. The same double gate holds for `mc` (23 sites) and for
   > `dct` (13 sites, **all coefficient-gated** — the `pDct` operand is
   > `md::coeff_level(pMbCache)`, F103's shape). So **`common/sad_common.rs`,
   > `common/mc.rs` and `encoder/sample.rs` reach `deny` after family 3, not with this
   > family**, and the plane family's own remainder is the reconstruction write:
   > intra-pred (5 sites), idct (10) and 17 of the 21 copy sites.
   >
   > Order, therefore: **plane roots (they unblock nothing on their own) -> family 3 ->
   > each table flips in one commit.** **Family 3 landed in session D**, so every one of
   > those tables now waits on the plane half *alone*: `pfSampleSad`, `pfSampleSatd`,
   > `pfSample4Sad`, `mc`, and the encoder's `pfDct*`/`pfIDct*` slots. The
   > fourteen coefficient slots that had *no* plane operand flipped with family 3
   > (T9.D8, T9.D10) — so **F103's split is fully closed**: 5 direct + 14 slot = 19.

   The **reconstruction** picture is *written* by every worker of the MT fork, disjoint at
   macroblock granularity but **not row-contiguous** (a slice can start mid-row), so it cannot
   be split into one `&mut [u8]` per worker — F73 is exactly that retag race. Session B
   measured it (**F107**): of the four MT configurations only `SM_FIXEDSLCNUM_SLICE` with RC
   on is row-aligned, so `split_at_mut` bands cannot be the mechanism; a worker reads only
   what it wrote (slice-scoped `uiNeighborAvail` and `uiFilterIdc`); and **no narrowing of a
   `&mut [u8]` expresses the disjointness**, because a macroblock's contiguous span is the
   full width of its rows. **Decided: D-mt-3 (option A)** — a shared interior-mutable plane view built once before the
   fork under one tagged `unsafe impl Sync` (a lawful `recon-seam` category, D-mt-1's shape), safe
   consumers, a Miri data-race probe; option (C), the raw-boundary scope reduction, was named and not taken. Session C converts the 32 blocked call sites plus deblocking against it
   and un-ignores the MT Miri probe — **and adds a second probe on a mid-row boundary**,
   because the existing one drives the row-aligned mode.

   ~2–3 sessions (B spent on the census and the design; **B2** carries the source/reference roots *and the table flips*, now that family 3 has landed; **C** the reconstruction consumers).
3. **SMbCache / SMB** (45 pure / 48 total, **+ the 14 coefficient slot types and their 59
   call-through sites**, + Phase 6's 72 kernels-take-slices note) — per-macroblock metadata;
   the encoder's `MbGrid` analogue. Bigger than the charter first read: ~2 sessions.
   **DONE in one (session D)** for `SMbCache`: all 41 parameters, the ten accessors, and
   all 14 slots. Its remainder is **not** family 3's — the 32 roots and the 31
   neighbour-bound `SMB` parameters are the layer's (F112, T9.D9), and the 3 unflipped
   slots are the plane's (F104).
4. **Layer / picture / slice pointers** (47 pure / 55 total + the 61 `cursor` accessors minus
   the 22 survivors) — `SDqLayer`, `SPicture`, `SSlice` reached as owned storage. ~1–2 sessions.
5. **The 22 dispatch survivors** — de-virtualize the five self-referential `SWelsFuncPtrList`
   slot types (`PDeblockingBSCalc`, `PDeblockingFilterSlice`, `PMotionSearchFunc`,
   `PSearchMethodFunc`, `PLineFullSearchFunc`), the way Phase 4a de-virtualized `pfMdCost`.
   Independent of the cursor spine. ~1 session.
6. **The `*mut sWelsEncCtx` conversion** (111 pure / 234 total) — unblocked once 1–4 land. Run
   `q1c.py` (rebuilt T9.A2 — session J's never existed, F102; widened T9.D5, F111) as the
   **precondition** (F66's instruction) — **308 sites / 71 callers at session D's close**, and
   calibrate it first (S55) — and split
   the context into sub-borrows (section 2.2.6) only where a caller must hold a sub-borrow
   across a call. The MT `send-seam` and the 21 MT tags retire here (D-mt-1). ~2–3 sessions.
7. **Crate-root cleanup** — `#![deny(unsafe_code)]` at the root (api keeps its tagged
   allows); drop `libc`; clippy `pedantic` triage; the module `deny`s become redundant with
   the root and the ratchet's job narrows to watching `api/`; final unscoped Miri and (if
   built) fuzz marathons. ~1 session.
8. **Inherited findings** — fold in where they fit, or a dedicated session: **F91** (upstream
   writes into the caller's `const` bitstream in parse-only SVC — the port does not; decide
   whether to match), **F92** (subset-SPS rewrite bounds — reachability probe), **F93** (four
   parse-only rows on damaged streams), **F100** (`LogStatistics`/`TraceParamInfo` empty
   bodies — pure `WelsLog`), **F84** (two orphaned threaded-decoder functions + 18 pad
   kernels — S18 deletions), **F86's open half** (the unchecked `pShortRefList[iRefIdx + 1]`
   write). Referees for these are built and green (8b).
9. **The end-of-phase decisions** — **D4** (workspace split into a `#![forbid]` core + the
   cdylib boundary — now a mechanical extraction, Phase 8 made the boundary precise) and
   **D5** (the Wels/Hungarian -> idiomatic rename) go to the user with the phase's final
   state in hand.

## 5. Estimate

**12–17 sessions** *(was 10–14; re-estimated by session A against the census).* This is
the encoder's equivalent of Phase 5 (the decoder took 29 from scratch), but the encoder
starts with ownership done (Phase 6), the vocabulary built (Phases 1/5), and the hazards
already found and named (F66/F73) — so it is closer to Phase 6's 10-session scale than
Phase 5's 29. The uncertain part is still family 6 (the ctx conversion under F66);
families 1–5 are mechanical caller-side conversions against proven types.

Three things push the number up, all from F101 and F103:

* **272 sites the charter counted as free are not.** "Body-only fns go safe when their
  callees do" is true, but it covers 147 sites, not 419.
* **`other` (123 sites, 80 pure) had no session.** It is seven unrelated sub-groups and
  needs roughly 2 sessions of its own — though being independent of the cursor spine, it
  can run in parallel with B–E rather than after them.
* **Family 3 grew.** It absorbs the 14 coefficient slot types and their 59 call-through
  sites on top of its own 45, so it is ~2 sessions rather than 1.

Session A itself came in under budget on conversion (5 kernels, not 25) and over on
mapping, which is the right trade for the first session of a phase: the map is what the
other twelve execute against, and both of the numbers it was given to check turned out
to be wrong.

## 6. Standing rules that bind every session

- **D-gate-1 / D-gate-2**: no mid-phase perf measurement; one battery at the phase close;
  the *unscoped* Miri once at the phase exit (both codecs' modules) — **amended by D-gate-4
  below**: the encoder-scoped `--lib` step also runs at every session close.
- **D-gate-3 does not apply** — that was Phase 8b's parity-gating. Phase 9 changes code that
  byte gates and Miri both watch, so it runs `gates.sh commit` per commit and, **at every
  session close, `MIRI_SCOPE=encoder bash rust/tools/gates.sh session`** — the sweeps in both
  profiles *and* the encoder-scoped Miri `--lib` step, no benches (**D-gate-4**, 2026-08-22,
  the user, on F114: session D's ten commits were byte-identical five times over and an
  encoder-scoped Miri run afterwards found a retag the session had introduced — a slot that
  now takes `&mut [i16]` of the whole `sCoeffLevel` kills every raw cursor held across the
  call; only Miri sees the class). **≈18 min** — 2.5 min family + Miri 894 s measured at
  `0bfc7687` (274 tests, the four encoder probes are most of it); not the ~3 min Phase 6's
  figure suggested, and still the cheapest instrument that sees the class. A session that
  touches decoder code drops the scope. The unscoped Miri, the differential Miri and the benches stay at the phase exit.
  *(A session that only deletes shims and converts callers byte-for-byte may justify
  commit-level gating between closes and say so — but the sweep is cheap insurance against
  a cursor conversion that moves a byte, and the Miri step at the close is not optional.)*
- **The referee is the tree, not the reference**: these are safety conversions with no
  intended behaviour change, so **every commit is byte-identical** on the goldens, the
  corpus, and both benches. A moved byte is a defect, bisected across the session's commits.
- **S20** (signature-reachability closure as the commit unit), **S37/S40** (a converted
  cursor's root must be retag-stable, with the Miri test), **D-exit-1** (any *new* raw
  signature is tagged; the count only goes down), **the ratchet decreases or is rebaselined
  with a reason in the same commit**.
- **Run `q1c.py` (the F66 detector) before any `*mut ctx` conversion**, not after — and
  **before scoping any family, aim it at that family's own root type** (`--type NAME`) and
  **calibrate it on a site known to be hazardous in the house spelling** before believing a
  zero (S55, F111: it read 0 with nine live sites in it because `addr_of_mut!` was not a
  derivation to it). After a conversion, `--kind ref` is the audit.
- **At session close, regenerate the census** (`python3 rust/tools/phase9_census.py --write`,
  and `phase9_plane_callers.py` if the plane family moved) and commit it with the log entry —
  it is the map the next brief is scoped from, and session D closed with it 49 tags stale.
- **Measured cost of the per-session gate** (session D's close, this machine): `gates.sh
  family` ≈ 2 min, the sweep ≈ 40 s per profile — cheap enough to run per *risky* commit,
  which D did five times; do not budget an hour for it.

## 7. Exit conditions

1. Every module outside `src/api/` carries `#![deny(unsafe_code)]` **and has zero
   `#[allow(unsafe_code)]` items** except the lawful boundary categories, which by Phase 9's
   end are only `C-ABI`/`C-ABI(test)` (all `port-raw`, `cursor`, `MT`, `send-seam` retired),
   **`recon-seam` (2 items, D-mt-3's ruling — the exit condition admits them by that
   decision, and `send-seam`'s 1 retires into the ctx conversion while these do not)**
   and `SCREEN_CONTENT(dormant)` (Phase 10's).
2. `#![deny(unsafe_code)]` at the crate root; `libc` removed from `[dependencies]`; `*mut`/
   `*const` appear only in the ABI types and the boundary thunks (a grep census proves it).
3. **A per-slot READ grep over every dispatch table** (F139): a slot that is installed and asserted-installed but never called is an S18 candidate, enumerated — at C2's close that list is `pfIDctT4`/`pfIDctFourT4`/`pfIDctI16x16Dc`, 8 of the 10 deblocking slots, and `pGomCost`.
4. `gates.sh exit` PASS unscoped — sweeps both profiles, both benches bit-identical, Miri
   `--lib` whole library, the differential tests, the gtest allowlist still exactly the 7
   Phase 10 rows + D-poc-1.
5. The perf position restated against D-perf-4's +25% tripwire, and **D-perf-6's parked
   recovery** taken (the Phase 5/6 parked families revisited now that dispatch is direct) or
   explicitly re-deferred with the user.
6. D4 and D5 put to the user with the final state; the ratchet's remaining job (watching
   `api/`'s size) documented.

## 8. Sessions

Sizes are the census's (`phase9_census.md` §6): `pure` is what the session can convert
unaided, `total` its workload once the earlier families have landed.

| session | scope | size | brief |
|---|---|---|---|
| **A** ✅ | census + the `q1c.py` detector rebuilt; the coefficient family converted | **19 → 5 done, 14 moved to D** | [`phase9_session_a.md`](phase9_session_a.md) |
| **B** ✅ brief [`phase9_session_b.md`](phase9_session_b.md) | plane family, part one. **The caller census (F104) and the reconstruction-write design (F107) landed; the conversions did not, and the census is why** — every cost-table site pairs a plane operand with an SMbCache one, so the tables wait on family 3 (see §4.2). Also: `WelsCalcPsnr` takes plane cursors (the one site with no second gate — F109, the snapshot is load-bearing), `expand_pic.rs`'s three dead shims deleted (F106); `cpu_core.rs` / `expand_pic.rs` / `wels_common_defs.rs` under `deny`; F105 (20 dead `mc.rs` kernels), F108 (deblocking runs *inside* the fork) | census + design; 3 files denied, 4 items deleted | — |
| **B2** ✅ *partial — B3 continues* — brief [`phase9_session_b2.md`](phase9_session_b2.md) | plane family, part 1b — the part B could not reach: `SPicData`'s source/reference halves become handles + coordinates, `SDqLayer` gains a source-picture handle so the ten layer-only consumers can reach the spatial pool (`phase9_plane_census.md` §5), the 12 `src` x `ref` cost sites are made ready — **and, family 3 having landed, the double-gated tables flip here, one commit each** (F104 has one gate left): `pfSampleSad`/`pfSampleSatd`/`pfSample4Sad` (37 sites), `mc` (23), the encoder's `pfDctT4`/`pfDctFourT4`; `common/sad_common.rs`, `common/mc.rs`, `encoder/sample.rs` reach `deny`. `pfIDctT4`/`pfIDctFourT4` write the reconstruction plane and are C's. **Re-aim `q1c.py` at the plane roots' owners first (S55), and give it F114's two shapes** (C: a raw held across a safe borrow of the same field; D: a reference argument protected across a call that re-borrows what it names) before converting anything; the reconstruction halves of `SPicData` (`pCsMb`/`pDecMb`, 42 uses) are C's, not B2's. Closes on D-gate-4's `session` gate. **Scoped 2026-08-23 (the user chose the wider cut): the brief carries the carrier, the ME cursors, all three flippable table families and the mc.rs de-shim.** Three traps measured into it — (a) the source pool is **not** read-only in the loop (`VaaBackgroundMbDataUpdate` writes the *previous* source picture per macroblock from inside the fork, `svc_mode_decision.rs:623` → `wels_preprocess.rs:1840`), so a shared `&SrcPicPool` cannot serve both halves — decide it or leave those three sites tagged for C; (b) **both instruments are stale or blind** and step 0 repairs them (`phase9_plane_callers.py`'s classifier keys on the ten accessors session D deleted — **40 unclassified, 0 coeff** at `2f5a2834` against B's 0 and 13, and it exits 0; `q1c.py` still cannot see F114's shapes C/D, which this session creates at scale); (c) two in-loop `ctx_pic_ref_mut(..).planes()` sites (`svc_mode_decision.rs:1842`, `:1899`) are F73's retag already in the tree. MC needs no slot flip — Phase 4a made it direct (33 call sites), so it is a caller conversion plus 22 dead shims deleted (F105). Observations to record, not act on: `WelsMdP4x4`/`P8x4`/`P4x8` are dead in the port and `#if 0` upstream (~210 lines, 6 `SPicData` reads); `SMcFunc` is filled by both codecs and read by neither, but is ABI-pinned. Likely two sessions (B2/B3) **Closed 2026-08-23 at `7917aa0b`, three commits, steps 0-1 whole and step 2 for one function.** T9.B20 repaired both instruments — the plane census had drifted 40 sites into `?` and 13 coeff into 0 and *exited 0* (F116; the regenerated tables are byte-identical to B's, so only the tool had rotted), and `q1c.py` gained shapes C and D, calibrated against `0bfc7687^` to report F114a's four bodies and F114b's two exactly. T9.B21 gave `SDqLayer` `pEncPic` + `pSrcPool` + `layer_enc_pic` (the ten context-free consumers' route), deleted `layer_ref_pic_mut` (S18), pin 824/752 → 840/768. T9.B22 converted `WelsMdPSkipEnc`'s three motion compensations to cursors. **Three of the brief's structural claims were wrong and each changes the remaining work**: (a) F117 — trap (a)'s copy runs the other way, its destination is the *current* source picture (`PCopyFunc` is `(pDst,..,pSrc,..)`; probe: `pEncPic == GetCurrentOrigFrame` 18/18 frames), and the harness disables background detection so **no gate runs the path at all** → the three sites stay raw, S57; (b) **F118 — steps 2 and 4 cannot be sequential**: `PlaneCursor` has no `as_ptr` and `root_ptr` is `&mut self`, so operand and slot must move in the same commit — but MC needs no slot (Phase 4a) and the cost tables are provably constant after `WelsInitSampleSadFunc`, so a constant-index site may call `sample_sad::<W,H>` directly and byte-identically, leaving only the 13 `md_cost`/`me_cost` runtime-selected sites in the forced single commit; (c) F119 — the 22 'dead' `mc.rs` shims have a caller, `tests/kernels_differential_phase2.rs` (13 of them), which the brief's greps scoped out — deleting them spends kernel parity coverage. F115 (`WelsMdP4x4`/`P8x4`/`P4x8` dead in the port, `#if 0` upstream) recorded undone: the user's call. Close `MIRI_SCOPE=encoder gates.sh session` PASS, 535/535 both profiles | 120 `SPicData` uses; **45 `pEncMb` / 23 `pRefMb` remain**; 37 + 20 + 2 table sites | — |
| **B3** ✅ — brief [`phase9_session_b3.md`](phase9_session_b3.md) | plane family, part 1c — the campaign B2 measured. **Eight commits `d4e930d4`..`15b20607`, all byte-identical.** Both rulings executed: **D-dead-1** deleted the sub-8x8 trio (−209 lines, −3 tags — all `cursor`, not F115's predicted 4 `port-raw`); **D-cov-1** is re-scoped rather than done (F124). **F118 solved by carrying both shapes**: T9.B25 made the three cost slots safe (`fn(&PlaneCursor, &PlaneCursor) -> i32`) *beside* a transitional raw triple filled by the same `WelsInitSampleSadFunc`, so each later commit converts one function instead of all forty-five at once; the raw triple and the two re-pinned ABI asserts (`SSampleDealingFunc` 176→344, `SWelsFuncPtrList` 1072→1240) retire with the last raw reader. Converted: `WelsMdPSkipEnc` + `AcceptPskip` (T9.B26), `WelsMdInterMbRefinement`'s twelve chroma MCs and three SADs (T9.B28), `MeRefineFracPixel`/`MeRefineQuarPixel` — **`md.rs` leaves the plane census entirely** (T9.B29), and the four intra cost sites against `SPicData`'s new `iMbX`/`iMbY` carrier coordinates (T9.B30, `SMbCache` 5584→5600). **Five findings, three of them corrections to this row's own numbers**: F120 the census never counted `md.rs`'s ten MC sites (30, not the 20 this row said); F121 `CalUVSadCost` + `JudgeStaticSkip`/`JudgeScrollSkip` are **dark** — probe 0 against a calibration probe reading 300–2136 — so step 1 item 3 is refused under S57 and **B2's two `ctx_pic_ref_mut(..).planes()` retags are live as code, unreachable as behaviour**; F122 three of `WelsMdInterMbRefinement`'s seven arms are unreachable because D-dead-1 took the producers and left the consumers; F123 `q1c.py`'s shape C matched `&` as well as `&mut` and over-reported (narrowed, recalibrated against `0bfc7687^` — still exactly F114a's four bodies); F124 the D-cov-1 correction is right about the C++ (no linkage, `libc` alone) and wrong about the count — **two** mc tests survive, the second being Phase 4a's dispatch assert-map. **`q1c.py` also caught a real defect the byte gates could not**: `&mut SMbCache` beside a raw derived from that arena is shape B at all seven `MeRefineFracPixel` sites; fixed by passing a `usize` offset (S54). Seven planted faults calibrate the seven new conversion shapes (chars 9244/7549/9208/9580/9201/424/58), all reverted. Close `MIRI_SCOPE=encoder gates.sh session` — 535/535 both profiles, Miri encoder scope green | 68 → **50** `SPicData` source/reference reads (12 stamps, **18 dark**, 8 ME, 10 DCT, 2 intra-pred); census 70 sites / 25 safe-now = 17 dark + 7 ME + 1 preprocess; ratchet raw_ptr **−181** | — |
| **B4** ✅ — brief [`phase9_session_b4.md`](phase9_session_b4.md) | plane family, part 1d — **the referee, then the campaign it turned out not to unlock.** Five commits `fa5a51f3`..`HEAD`, all byte-identical. **D-ref-1 landed**: `bEnableBackgroundDetection` reaches both drivers as a 20th positional argument and a 48-row `bg` preset joins the exit list — the first byte coverage `WelsMdBackgroundMbEnc`, `VaaBackgroundMbDataUpdate`, `WelsMdUpdateBGDInfo` and the analyzer's `BackgroundDetection` have ever had, on a flag `FillDefault` leaves ON. Calibrated both ways before any verdict was used: entry 1159–5771/row against **0** with the flag off, a planted one-sample fault failing 32 of 48 rows against `sl` 12/12. **Parity work found: none** — 48/48 both profiles, unplanted, first run. **F126 is the calibration's real result**: all 16 `Static_152_100` rows pass the planted fault *and* still pass at `+128` — on that clip the background prediction is byte-inert (all `MB_TYPE_BACKGROUND` P_SKIP, decisions taken from source-domain VAA planes), so **4524 entries and zero byte sensitivity in the same row**. Quote 32 rows of MD coverage, not 48. Those rows still referee `VaaBackgroundMbDataUpdate`, which fails on all three clips — session C's three sites are covered. **F125 confirms this row's own re-scope and prices it**: the preset lights 8, the other 9 are screen-content-gated and were **retagged, not converted** — tags 696 → 688, `SCREEN_CONTENT(dormant)` 8 → 16, reclassification with no conversion behind it. Probed: `SvcMdSCDMbEnc` reads 0 in every `bg` row, including the one where `WelsMdBackgroundMbEnc` enters 5771 times. **T9.B4's five conversions** (3 MC + SAD + SATD) run on T9.B22's route; census `safe-now` **25 → 20**. **D-dead-2** deleted F122's closure, **two items larger than F122 listed** (`SWelsMeContainers` — a whole second declaration of `SWelsMD_sMe` with zero references; the `UpdateP*Motion2Cache` trio with zero call sites), `assert_size!(SWelsMD, 4000)` re-pinned to **928**. **D-cov-1 closed**: 26 shims, **three** tests and three dead `unsafe extern "C" fn` typedefs go, `SMcFunc`'s six slots are retyped to the safe kernels' signatures (pin 48 holds), `common/mc.rs` reaches `deny` with **three** dormant-tagged allows. **F129's generalisation for S18**: a straggler sweep in this crate **cannot** be a compiler pass — stripping `#![allow(dead_code)]` from every module and rebuilding reports 14 dead items crate-wide and **none of this closure**, because every item is `pub` in a `pub mod`. It is a per-symbol reference grep. **Where this row's brief was wrong** (F127/F130): `gates.sh` claimed 505 sweep rows against the prose's 535 — measured **535**, now **583** with `bg`; the Phase 4a assert-map is in `tests/kernels_differential_phase2.rs`, not `mc.rs`; a **third** mc test existed that neither the brief nor F124 had; `mc.rs` needs three allows, not two; and the 17 `SUB_MB_TYPE` mentions are in the two syntax writers, not three files (`svc_encode_mb.rs` has zero). Close `MIRI_SCOPE=encoder gates.sh session` — **583/583 both profiles**, Miri encoder scope green | census 70 sites / **20 safe-now** (9 of them dormant-tagged) / 13 coeff / 32 blocked; tags **688** = 631 `port-raw` + 57 `cursor`, `SCREEN_CONTENT(dormant)` **19**; `SPicData` code reads **97 → 92**; ratchet raw_ptr **−245**, shim **−46**, unsafe_fn **−81** — **all of the session's −64 is D-cov-1's**; the conversions and D-dead-2 each measured *zero* ratchet delta (the metric counts `*mut`/`*const` spellings, and neither deleted any), so quote the plane census for the conversions and the line count for the deletions | — |
| **C** ✅ *partial — C2 completed it* — brief [`phase9_session_c.md`](phase9_session_c.md); **D-mt-3 confirmed by the user 2026-08-23, option A** | plane family, part two — **the seam, its roots, and the measurement that says who owns the acceptance.** Seven commits `85a525d3`..`bdf425fd`, all byte-identical, 583/583 both profiles. **The seam is a *picture* view, not a plane view (F131)**: of the 14 in-frame `layer_dec_pic*` call sites only 4 reach a plane — the other 10 reach four per-macroblock side arrays (`sMvList`/`pRefMbQp`/`pMbSkipSad`/`uiRefMbType`), where a `&mut Vec<T>` is F107 §3's whole-range retag verbatim, and the *first* thing Miri reports on the ignored probe is a race on `SRefList` itself, not on a pixel. `encoder/rec_view.rs` carries all seven storages behind **one** `UnsafeCell`-crossing accessor and **one** `unsafe impl Sync`, both `recon-seam` from the first commit; four probes green under Miri and both threaded ones calibrated by planting the overlap. Placement: the layer (`SDqLayer.pRecView`, built in `WelsInitCurrentLayer`), so no signature moves. **`layer_dec_pic`, `layer_dec_pic_mut` and `SPicData.pDecMb` are all deleted** — `pDecMb` after being *proved* equal to `pCsMb` over 583 rows x 2 profiles with a calibrated `debug_assert` (F134); pins `SDqLayer` 840/768 → 1008/936, `SMbCache` 5600 → 5568. **F132 is the session's headline: the fork contends over six shared families, not one**, enumerated by fixing what Miri names and re-running — the reconstruction picture (C's), `SStrideTables`, `SWelsSvcRc::pGomCost`, `SDqLayer::sSliceEncCtx` (all four fixed here), then `&mut SMB` vs deblocking's cross-slice neighbour read (**session E's**, F112/F114b) and `pOverallMbMap` rewritten in-fork (**the slice structures'**). **So F107's acceptance was assigned to the wrong session** and cannot be met by the owner of the reconstruction write. **F133: `pGomCost` is a genuine data race in upstream OpenH264** on storage nothing reads — five references each side, not one a read — made `AtomicI32` here, deletion left as a ruling. The **mid-row probe** is built, passes as a behaviour test, and asserts its boundary from the bitstream (`EncodedFrame::first_mbs` reads `first_mb_in_slice`); calibrated against the row-aligned mode, which fails it with `[0, 28] at 7 macroblocks per row` — F107 §1 confirmed a second way. It drives `EncodeSizeLimitedSlicesForked`, **a third fork the brief did not list**. **T9.C7** converts `WelsRecPskip` through `copy_block_to_view`, the first of F107 §3's three write-through-`&self` kernel families; planted fault fails 150 of 210 `st` rows. **F135**: the census's `WelsRecPskip` 6 is 3 live + 3 **dead** (a second implementation with no caller), left in place so no census row is credited to a deletion. `mc.rs`'s dead kernels were already deleted by B4 (D-cov-1), not by C. Close `MIRI_SCOPE=encoder gates.sh session` at `08c98c47` — **583/583 both profiles, Miri 277 passed / 0 failed / 5 ignored in 1012 s** (+34 s on B4's 978 s; the four seam probes are 1.84 s of it, so most of the delta is variance — the encoder-scoped set is 282 tests, +5 this session and nothing removed, verified by diffing the test list against `9fa8cde3`) | tags 779 → 779 (port-raw 631 → 629, `recon-seam` **+2**); blocked **32 → 29**; `layer_dec_pic*` sites **14 → 0**; raw_ptr 2004 → 2001 | — |
| **C2** ⚠️ *code complete, close gate unverified (F140)* — brief [`phase9_session_c2.md`](phase9_session_c2.md) | the seam's consumers — **every blocked site in scope converted, 27 → 3, and the 3 are Phase 10's.** Eight commits `e1cb5ad9`..`41ff57ad`, all byte-identical, 583/583 both profiles at every family gate. **Step 0a was two fields, not one (F136)**: `pOverallMbMap`'s 18 sites were exact but did not meet the brief's own acceptance — with only the map atomic the mid-row probe still stopped inside `SSliceCtx`, at `iSliceNumInFrame` (`sSliceEncCtx`@320 + 28 = the `0x15c` Miri named). The read that reaches it is the *whole-struct* shared retag at `svc_encode_slice.rs:1982`, so the fork contends with the struct rather than a field; the enumeration terminates only because exactly one of thirty `SSliceCtx` scalar writes is in-fork. **F69's mutex stays** — it brackets `AddSliceBoundary`'s map rewrite *with* the increment, and what it never did was help the readers, none of which take it. With both fields atomic **round 6 closes and the probe advances exactly as predicted**, to round 5's `&mut SMB` vs `deblocking.rs:1190`'s `uiSliceIdc` — **so both encoder fork/join probes now stop on the same family**, and one conversion un-ignores two probes. **F137: the census could not see `pfIDctI16x16Dc`** — neither the slot nor its kernel was in `phase9_plane_callers.py`, so `WelsEncRecI16x16Y`'s DC-only branch was invisible; corrected, and the tool gained a `seam` class so a converted site reads as converted rather than as unclassifiable. **F138 and F139: both remaining kernel families converted in one pass each**, because each had its raw surface in a handful of helpers — intra-pred's three (`pred`/`top_row`/`reference`) and deblocking's twelve one-line shims. Two traits carry it: `RefSamples` (`at`+`row_n`, read-only — 45+6 uses measured) and `PlaneSamples` (`+set` — 22+18), two impls each, static dispatch, **one kernel set still serving the decoder and the encoder**. `common/intra_pred_common.rs` and `encoder/get_intra_predictor.rs` reach `#![deny(unsafe_code)]` with **zero** allows; the three intra-pred tables are safe and **split by prediction size** (F113's shape again). **~370 lines of Miri span probe retired** with the shims they interrogated, each of its three properties accounted for (D-cov-1). **Three write-only dispatch tables found** — `pfIDct*` (after T9.C2g deleted the helper T9.C2d orphaned) and 8 of the encoder's 10 `DeblockingFunc` slots, joining F133's `pGomCost`; all left as measured, and F139 asks the phase-exit checklist for a *read*-grep per slot. **Neither `deny` step 6 promised is reachable and the two owners differ**: `encoder/deblocking.rs`'s 13 remaining allows are all `*mut SMB`/`*mut SDqLayer` parameters (**session E's**), `common/deblocking_common.rs`'s 12 shims are the **decoder's** table. `PCopyFunc` and `common/copy_mb.rs` wait on F117 *and* Phase 10. **The close gate never produced a Miri verdict (F140)** — three attempts, one killed by the user at 1.5 h. Cause measured, not guessed: the seam costs **2.6x** in Miri wall-time (`encode_loop_runs_over_a_macroblock_grid` 96.5 s → 254.3 s; the dynamic-slices probe 422.9 s → >19 min), *uniformly*, because every reconstruction access is now a bounds-checked `Cell` index that Miri interprets one at a time. Two plausible causes were disproved by experiment (cursor construction — hoisting moved it 0.1%; the atomics — the grid probe has no slice boundaries and regressed anyway), so **there is no hot spot to hunt**. The actionable part is that **no gate measures Miri wall-time**, so a 2.6x regression in the phase's principal safety instrument passed eight commits unremarked; F140 asks for a per-close wall-time ratchet and names the two encode probes as the whole cost. The family battery passed twice (583/583 both profiles), so correctness evidence is complete and **aliasing evidence at session scope is not** — treat the encoder Miri set as unverified since `bdf425fd`. Eight planted faults, one per conversion shape, 16–210 rows each — and **`WelsMdBackgroundMbEnc`'s referee is 16 of 48, not F126's 32**, because F126 planted before the mode decision and this session plants in the final copy. | 24 sites + deblocking; blocked **27 → 3**; tags 779 → 730 (port-raw 629 → 580); ratchet raw_ptr **−110**, unsafe_fn **−44**, unsafe_block **−58**, shim **−28** | — |
| **D** ✅ brief [`phase9_session_d.md`](phase9_session_d.md) | SMbCache/SMB, **the arena — landed whole**. The ten raw accessors are deleted (six spelled at the site, four ping-pong halves are `const fn`s over the flag); **all 41 `*mut SMbCache` parameters are `&mut SMbCache`** (`grep ': \*mut SMbCache' src/encoder` = 0); **all 14 of F103's slot-only coefficient kernels hold safe function pointers** and their shims are gone (T9.D8 eleven, T9.D10 the three dequantisers T9.D8 had wrongly written off as decoder-shared). 6 of 42 `*mut SMB` converted; the other 31 are neighbour-bound and belong to the layer. **The detector was wrong twice** (F111): it could not see `addr_of_mut!` derivations, nor a root minted from an owned field — 22 `SMbCache` sites and **20 context sites** were hidden, so §4.6's "287/68" is stale twice over (**308 sites / 71 callers** today). Also F110 (the brief's `WelsEncRecUV` claim was false — the two callers use different bases), F112 (the arena's *roots* must stay raw; a `&mut` to the field is popped by any callee that re-derives it — T6.C2's Miri verdict, generalised), F113 (the residual slots' raw types hid three things, one of them a single type serving two lengths). **Closed at `0bfc7687` after T9.D11 — F114**: the first encoder-scoped Miri run over the finished session found two UB the byte gates had passed — a raw derived by `addr_of_mut!` under the now-`&mut` parent, popped by a flipped slot's whole-field `&mut` borrow (four bodies); and T9.D9's `&SMB` arguments protected for the call while a nested callee re-borrowed the same macroblock. Fixed by removing pointers. Close `MIRI_SCOPE=encoder gates.sh session` PASS: 535/535 both profiles, 548/541, Miri 274/0 — D-gate-4's first green run | 41 params + 10 accessors + 14 slots + 6 SMB; raw_ptr -164, unsafe_fn -52, shim -20 | — |
| **E** ✅ *steps 0–3 + the acceptance; the flips are E2's* — brief [`phase9_session_e.md`](phase9_session_e.md) | **The acceptance landed: both fork/join probes un-ignored and GREEN — the first complete MT encodes under Miri in the project's history** (T9.E8: 3356 s / 3449 s, ~57 min as a parallel pair; F132's enumeration closed at round 9). Eight commits `17b943a4`..`b29b0625`, byte-identical throughout (583/583 both profiles at every family gate). **D-gate-6 mid-session (the user): the whole session gate is capped at 15 minutes** after the first post-shrink run was stopped at ~40 — met by T9.E2's re-architecture (a parallel five-shard Miri lane beside the native battery; the encode probes drive small geometries, `MIRI_FULL=1` restores them at full/exit, the fork probes are name-skipped at session scope with the explicit-run recipe) and validated at **542 s** — which is also C/C2's clean-run evidence at session scope. **Round 5 closed as an outcome-equality substitution** (F142): deblocking's guards read `pOverallMbMap` instead of the neighbour's `SMB` record — the brief's value-assert was the race it proposed to measure, and validation's idc 0→2 rewrite is why deblocking is in-fork at all. Closing round 5 unmasked rounds 7–9 (F143: the per-slice `pfInterMd` stamp, four in-fork `as_mut_ptr` autoref mints, the `sLayerInfo` array-autoref mint) — F132's six rounds were a frontier, not an inventory. **Step 3 landed whole: `q1c --type SSlice` 68 → 0** (accessors/stash/GetBsPosition/RC helpers narrowed to the fields they touch; per-use-cluster derivation windows in fourteen callers; the dispatch-slot `pfInterMd` crossing placed by hand where q1c is blind). F141–F144; the layer family's real pre-flip work is 3 arg-order sites, not 14 (F144's bool-cursor false positives) | 68 hazards → 0; rounds 5, 7, 8, 9; two probes un-ignored; gate 40 min → 9 | — |
| **E2** ◀ **next** — brief [`phase9_session_e2.md`](phase9_session_e2.md); **the user's gating directive (2026-08-24): no Miri between commits — the static detector between stages, the session gate once at the close plus one MIRI_FULL fork-probe pair** | the flips E's hazard work priced and unblocked, every commit now refereed by the live fork probes: **step 4** — 122 `*mut SSlice` parameters → `&mut SSlice` in one S20 closure with the 26 arena roots → field borrows in the same commit (F112's one step), the six slice-carrying fn-pointer types, one `&mut *pSlice` reborrow per worker entry, exit `q1c --kind ref` = 0 all shapes + a green explicit fork-probe run; **steps 5–6** — the layer flip (3 real B-sites then 73 params), the neighbour-bound `SMB` grid answer, `encoder/deblocking.rs`'s 13 allows, `sRefPicView`'s 21 reads, the ~34 reachable `cursor` tags (23 of 57 are `ctx_*`, G–H's) | 122 + 73 params; 26 roots; 35 SMB | — |
| F | the 22 dispatch survivors | 4 pure / 5 (+17 cursor) | — |
| **X1–X2** | **`other`: bitstream, paramsets, RC, LTR, VAA, deblock, trace, scalars.** New — the charter had no session for it. Independent of the cursor spine, so it can run in parallel with B–E | 80 pure / 123 | — |
| G–H | the `*mut ctx` conversion (**run `q1c.py` first** — **308 sites, 71 callers** as of session D; the older 287/288 figures predate F111's two fixes to the detector) + the MT seam/Send flip | 111 pure / 234 | — |
| I | inherited findings (F91/F92/F93/F100/F84/F86-open) | — | — |
| J | crate-root cleanup, libc, clippy, the Miri/fuzz marathon; D4/D5 to the user | — | — |

**Session A's products for everyone downstream:**
`rust/docs/phase9_census.md` (the map — read it first), `rust/tools/phase9_census.py`
(regenerate it; `--family <name>` lists a family's sites), `rust/tools/q1c.py`
(family 6's precondition, exit 1 on any hazard), `rust/docs/phase9_findings.md`
(F101 the census correction, F102 the detector, F103 the coefficient family's real shape).

**Session D's products:** `rust/tools/q1c.py` grown into a general instrument —
`--type NAME` (aim it at any struct), `--kind raw|ref` (the pre-conversion question or
the post-conversion audit), a **loud exit 2** on both empty cases, a callee table keyed
by `(name, file)`, and two false-negative classes closed (F111). In
`phase9_findings.md`: **F110** (the brief's `WelsEncRecUV` claim was false — check every
caller's base before deleting a cursor parameter), **F111** (the detector's two blind
spots, and family 6's precondition number moving to 308/71), **F112** (**read this
before converting `*mut SSlice`** — the arena's roots and why a `&mut` to an owned field
is popped by the callee that re-derives it), **F113** (what the residual slots' raw
types were hiding), **F114** (**read this before converting any parameter to a reference**: two UB the conversion introduced and no byte gate saw — `addr_of_mut!` under a parent that became `&mut` is popped by a safe borrow of the same field; a `&T` argument is a protector for the whole call. The detector's shapes C and D, not yet implemented).

**Session B's products:** `rust/docs/phase9_plane_census.md` (the plane family read
from the **callers** — every call site, every operand classified by the surface it
names, in-fork or not), `rust/tools/phase9_plane_callers.py` (regenerate it;
`--sites`, `--unknown`, `--internal`, `--handoff`), and in `phase9_findings.md`:
**F104** (the double gate — the census's headline, and what re-scoped §4.2 and §8),
**F105** (20 dead `mc.rs` kernels), **F106** (the dead expand shims, deleted),
**F107** (the reconstruction-write measurement and design — read this before
starting session C), **F108** (deblocking is in-fork under MT), **F109** (a handle
re-read at the point of use is not always the handle that was captured — read this
before starting session B2).

**The generalisation these three sessions have now made twice** (F103, F104):
`pure` and `safe-now` are properties of *signatures*, and a session's real start
set is the intersection with what its callers already hold. Sessions D–H should
build the caller view of their own family before scoping, not after.
