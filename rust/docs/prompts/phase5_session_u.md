# Phase 5, session U — the last session: instruments, W6, W7, W8 whole

This session **closes Phase 5**: T's two named instrument fixes, then W6, W7,
and W8 whole — the twice-postponed adjudication and the close. **D-par-1**,
**D-fid-1**, **D-gate-1**, **S31**–**S33**, forcing rules v2 in force. Counts at
`b0e74e8d`; re-grep at each face's open (S24). **W8 is never compressed**: if
the session cannot reach it whole, it hands it off intact — that is the one
sanctioned hand-off, and it must name what day-two reads remain.

## 0. Start

1. Commit the inherited doc tail (the mapping, this brief).
2. Open per **S27**: T closed `OVERALL: PASS` at exit, every step green → cheap
   subset if tools/toolchain unchanged. Last recorded: tests green at T's close,
   census 59, sweeps 341/341 both profiles, conformance 60/60, decoder
   `raw_ptr` **980**, `SHIM(` **52**, deny-clean modules **0**. Recount.
3. S32 on any probe change; S33 on every number written.

## 1. Face 0 — T's two instrument fixes (hand-off §3–4)

1. **The gate hole**: add `cargo build --all-targets` to `gates.sh commit`
   (~seconds). T5.T5 — a public type change breaking a bench invisibly — is the
   cost of its absence; this closes it mechanically rather than by rule.
2. **The 389-row referee**: `ecref` takes a byte blob on stdin, and the four
   families that build bytes inside the Rust harness (header-corruption,
   emulation-prevention, synthetic-tail, raw-feed) replay through it. Run all
   389 rows against the C++: agreements convert the rows' pins to C++-refereed
   (logged per table); divergences are **evidence** — fix one-liners, scope the
   rest as findings (F46's example: reduce to distinct causes with a minimal
   reproduction). Done-test: **0 corpus rows without a C++ referee**, or the
   residue enumerated with causes.

## 2. Face 1 — W6: `decode_slice.rs` goes clean

Per phase5.md §S-settlements (settled, waiting since session R):

1. **The view struct**: one `unsafe` constructor per bracket top (three), in
   `decoder_context.rs` — `&mut` for the raw-data reader, the CABAC engine, the
   flag/counter set; `&` for tables/config; copied scalars where S23 clears
   them (verify constancy per field, log the check); `pParam`'s scalars copied
   inside the constructor. The 44 functions take the view.
2. The EC MC paths per P1 (live since session S).
3. `cabac_rbsp_window`'s retirement (21 sites; the window is constant across a
   slice — the bracket maneuver again).
4. Signature-leg merges where they serve (D-fid-1; 148 is an upper bound).
5. Done-test: `decode_slice.rs` compiles under `#![deny(unsafe_code)]`, the
   constructor enumerated as the exception outside the file. Probe per seam.

## 3. Face 2 — W7: closure of the instruments

5.2's straggler sweep; **F40-class sweep** crate-wide (count operand × element
size); decoder `SHIM(` **52 → 1 named survivor** (`data_ptr`'s output-contract
consumer, Phase 8's); `#![deny(unsafe_code)]` per decoder module, exceptions
enumerated with Phase 8 pointers. Done-test: SHIM greps match the survivor list
exactly; every decoder module deny-clean or on the exception list.

## 4. Face 3 — W8 whole: the adjudication, then the close

**After the last code commit**, so the measurement window is closed. S2b
protocol; S33 on every number.

1. **Session N's stashed binaries** (`.perfpair/n_base|n_mid|n_head`): the owed
   3-pair reading — was N's +1.24% CB real, and where.
2. **The niche verdict**: build the `NonZeroU32` hypothesis binary (written at
   N's log entry), one pair against `n_head`.
3. **The D-gate-1 window as spans**: pre-window base → this session's last code
   commit (the window now contains N-tail, O–T, and U's own W6/W7), 3 pairs +
   null pair; bisect only if the span demands it (S5).
4. **The stop-line verdict**: cumulative CB from ≈+21.6–21.9% (at N) plus the
   window's spans, against ≈+23% (D-perf-4). Written either way; if over, the
   escalation table for Eugene — options with measured sizes.
5. **The ledger reconciled** toward the recovery expectation, or escalated.
6. **Day-two confirmation** scheduled; if the session cannot span days, the
   reading is labeled provisional and the hand-off names exactly what day two
   re-runs — the one sanctioned remnant.

Then the close: **`prompts/phase6.md` per S19** (the encoder brief inherits the
playbook: the ordering rule, cache-not-carrier, settlements-in-writing, the
probe budget and S32, S31, S33, and D-par-1's standing referee — `ecref` and
the corpus discipline travel to Phase 6); briefs stamped historical; this
checklist closed; §0 refreshed; open findings each with an owner (F3→Phase 7,
F23/F38-class/F41 + the `api/` inventory→Phase 8, F36→threading-or-deletion,
the `CABA2_SVA_B` tie-break annotation standing).

## 5. Gates

Per commit: build both profiles **+ `--all-targets` after face 0** + tests +
ratchet + census. Probe per seam, S32's arithmetic beside any addition. Full
battery once, at `exit` level, at close; F3 per S14. **Do not edit the working
tree while the battery runs.**

## 6. Close

The exit is the close (face 3). Log entry from breadcrumbs (≤ 30 lines), the
adjudication tables, phase5.md closed, phase6.md in place. Hand-off only if W8
was handed off whole.

## 7. Non-goals

No encoder sites (F12/P10 — Phase 6's). No F23/F38-class/F41/`api/` work
(Phase 8's). No F36 work. No `get_unchecked` (S8). No golden movement except
face 0's referee conversions (logged) and the standing `CABA2_SVA_B`
annotation. No re-opening settled designs. No trimming face 3 to finish — W8
hands off whole or lands whole.
