# Phase 9 — the ctx hazard x fork-reachability join (F161)

*Generated by `python3 rust/tools/phase9_ctx_join.py`. **Re-run it before quoting** —
the tool is the authority, this is the snapshot (S24). Regenerated at session G's close;
at G's step 1 the same run read 266 total / 131 live / 135 moot.*

**All 7 live sites remaining are analysed false positives — see F163.** Four are
`pParamD`, a cursor into the coding-param `Box` (a different allocation from the
context, so a `&mut sWelsEncCtx` entry retag cannot reach it); three are T9.G6's
argument hoists, where the binding *is* the call's argument.

```
join of q1c (hazards) x forksplit (fork-reachability), type sWelsEncCtx

    92  hazards reported by q1c --kind raw
     7  LIVE  — callee is ST-flippable, so the &mut retag really happens
    85  moot  — callee is in-fork, so S63 forbids the retag q1c models

LIVE: A=7 B=0, over 5 distinct held cursors (file, caller, binding)
     7  encoder/ref_list_mgr_svc.rs   A=7 B=0

moot: A=62 B=23, over 35 distinct held cursors (file, caller, binding)
    39  encoder/ref_list_mgr_svc.rs   A=35 B=4
    12  encoder/rc.rs   A=12 B=0
     9  encoder/encoder_ext.rs   A=5 B=4
     8  encoder/svc_encode_slice.rs   A=3 B=5
     5  encoder/slice_multi_threading.rs   A=1 B=4
     5  encoder/svc_mode_decision.rs   A=5 B=0
     2  encoder/svc_set_mb_syn_cavlc.rs   A=0 B=2
     2  encoder/wels_preprocess.rs   A=0 B=2
     1  encoder/encoder_context.rs   A=1 B=0
     1  encoder/svc_base_layer_md.rs   A=0 B=1
     1  encoder/svc_encode_mb.rs   A=0 B=1

LIVE, by held cursor — this is what step 2 retires:
     3  pParamD              in WelsUpdateRefList()   encoder/ref_list_mgr_svc.rs
     1  pCurLayerForMmco     in WelsMarkPic()   encoder/ref_list_mgr_svc.rs
     1  pCurLayerForSh       in WelsUpdateRefSyntax()   encoder/ref_list_mgr_svc.rs
     1  pParamD              in WelsMarkPicScreen()   encoder/ref_list_mgr_svc.rs
     1  pCurLayerForMmco     in WelsMarkPicScreen()   encoder/ref_list_mgr_svc.rs

LIVE shape B (0 argument-order hoists), by caller:
```
