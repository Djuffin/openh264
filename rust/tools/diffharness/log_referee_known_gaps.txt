# Trace messages the reference delivers and this port does not — the log referee's
# allowlist (Phase 9 session X2, F100). Same contract as
# `abi_harness/gtest_known_failures.txt`: the referee fails on any difference NOT
# named here, and fails again on a row here that has stopped differing (a stale
# row is a lie about coverage). The list therefore moves only in the commit that
# fixes the gap.
#
#   format:  <extended regex matching the message text> | <owner> | <reason>
#   blank lines and `#` comments ignored. The regex is matched against the
#   normalized message text with the `<level>|` prefix and the `[OpenH264] this =
#   ... , <Tag>:` header already stripped.
#
# Measured on the fixed row 2026-08-27 (session J, step 4): **reference 35 lines,
# port 33**, of which **33 are byte-for-byte identical** and the remaining two are
# the permanent pair below. The gap list is CLOSED: every row that was owned by a
# session is ported, and what is left cannot be ported for a stated reason.
#
# The series, for the record: X2 took the port from **1** identical message to 9,
# H2's `LogStatistics` coverage work took the capture from 19 to 35 reference
# lines and the port to 20, and J ported the last seven — the three
# `CheckProfileSetting` warnings, the CABAC->CAVLC follow-on, the frame-skip
# warning, both QP-range lines, `WelsInitEncoderExt`/`WelsUninitEncoderExt`'s
# pointer traces and the destructor's. With them the **delivered level sets agree
# too** (`[2 4]` on both sides): every level-2 line the reference emits is a
# `ParamValidationExt` warning the port now emits, which is what D-fid-4's
# original acceptance was reaching for and F189 could not express while any gap
# was owned.
#
# This header said "reference 19 lines, port 9" until H2, and "port 20" until J.
# A file whose whole contract is "a stale row is a lie about coverage" has now
# carried a stale coverage count in its header twice. The numbers a document
# states about an instrument are as stale-able as the rows.

# --- The two permanent rows: the allocator's accounting lines ---------------
# **This is the whole remaining gap, and neither row is portable.** `overall memory usage: N bytes` cannot be ported
# verbatim: this port has no `CMemoryAlign` running total to print — Phase 3-6
# replaced the arena with owned containers, which is the reason the number does
# not exist rather than an oversight. Its row is permanent unless a ruling says
# to synthesize a number, which would be inventing an observable.
WelsInitEncoderExt\(\) exit, overall memory usage: [0-9]+ bytes | permanent | no CMemoryAlign running total exists in this port (Phase 3-6 retired the arena)
FreeMemorySvc\(\), verify memory usage \([0-9]+ bytes\) after free\.\. | permanent | same
