# The `test/api` rows the Rust cdylib is known to fail — the allowlist behind
# `gtest_stretch.sh --check` (Phase 8b session A, T8b.A1).
#
#   format:  <gtest full test name> | <owner> | <reason>
#   blank lines and `#` comments ignored; the name must be the *full* name gtest
#   prints, parametrized instances spelled out one per row (`.../0`, `/1`, `/2`).
#
# `--check` fails when a failing test is NOT here (a regression, or a new failure
# nobody owns) and when a listed test PASSES (a stale row — a lie about coverage).
# The tally therefore moves only by editing this file in the same commit as the fix.
#
# Owners are the phase/session that will make the row pass. Rows are grouped by the
# *assertion site* they fail at (S47: a fixture name is a surface), which is stated
# in each group's header and re-measured by `--check`'s own by-site tally.

# --- the listing strategies: all seven rows gone (T8b.B3) --------------------
# `CWelsParametersetSpsListing`, `CWelsParametersetSpsListingPpsIncreasing` and
# `CWelsParametersetSpsPpsListing` are ported, along with `FindExistingPps` and
# `WriteSavcParaset_Listing`, so `encode_options_test.cpp:556/656/705/772/844/911`
# and `decode_api_test.cpp:666` all pass. The seventh row —
# `DecodeCrashTestAPI.DecoderCrashTest`, F89 — was only visible once T8b.B1 pinned
# the seed: it draws its strategy from `rand() % 7` and takes a listing one on
# three of seven draws.
#
# The referees, so a regression here is not just a gtest number: five targeted
# `cxx_enc`/`rust_enc` pairs, one per `eSpsPpsIdStrategy` value, byte-identical
# (the drivers' 18th argument, added in the same commit), plus five in-module
# tests in `paraset_strategy.rs` that pin the class-tree asymmetries — the ones a
# tidier `match` would quietly erase.

# --- decode_api_test.cpp:888 — parse-only: nothing left ----------------------
# T8b.B2 ported `DecodeParser` and its two siblings; `ParseOnly_General` was the last
# row here and it was never a parse-only failure — its encoder asks for three spatial
# layers, which `InitializeExt` refused under S48. **T8b.C2 ported
# `METHOD_DOWNSAMPLE` and it passes.** The parse-only referees stand:
# `tests/decoder_parseonly_parity_test.rs` against `ecref --parse-only` goldens, and
# `abi_harness parseonly` through the dylib.

# --- denoise / downsample: all seventeen rows gone (T8b.C1, T8b.C2) ----------
# `METHOD_DENOISE` (`processing/denoise.rs`) and `METHOD_DOWNSAMPLE`
# (`processing/downsample.rs`) are ported, so S48's two refusals in
# `ParamValidationExt` are gone and every row that sat behind them passes:
# `EncoderOutputTest/4` (denoise), `/5` and `/7` (2 and 4 spatial layers, the two
# rows that hash), the twelve `EncodeDecodeTestAPI` multi-layer rows, the three
# `GetOptionTid_SVC_L1_NOLOSS` instances and `ParseOnly_General`.
#
# **Those fourteen API rows had been passing before T8b.A5 by never looking**: they
# assert on codes, frame counts and layer ids, none on bytes, so lower layers built
# from whatever the picture pool last held went unnoticed. They pass now for the
# right reason — the referees are five targeted `cxx_enc`/`rust_enc` pairs, all
# byte-identical: 2 layers at 320x192, 3 layers at 320x192, **4 layers at 1280x720**,
# 2 layers + denoise, and denoise alone. Plus the `dl` sweep preset, which is the
# only preset that runs the downsampler at all.
#
# `/5` and `/7` carry *two* golden hashes each, upstream's hedge against
# vertical-vs-horizontal averaging order. The port needs neither indulgence: it is
# byte-identical to the reference, so it matches whichever of the two the reference
# produces. See F97 — the `_c` and NEON kernels agree bit for bit, and the real trap
# was the aarch64 dispatch *table*, which binds general-ratio luma to the accurate
# downsampler where the scalar table binds the fast one.

# --- BaseEncoderTest.cpp:92 / encode_options_test.cpp:2195 / encoder_test.cpp:323
# --- screen content ----------------------------------------------------------
EncodeFile/EncoderOutputTest.CompareOutput/8 | 10 | screen content: the D-scr-1 guard (encoder_ext.rs:817)
EncodeFile/EncoderOutputTest.CompareOutput/9 | 10 | screen content: the D-scr-1 guard (encoder_ext.rs:817)
EncodeFile/EncoderOutputTest.CompareOutput/10 | 10 | screen content: the D-scr-1 guard (encoder_ext.rs:817)
EncodeFile/EncoderOutputTest.CompareOutput/11 | 10 | screen content: the D-scr-1 guard (encoder_ext.rs:817)
EncodeFile/EncoderOutputTest.CompareOutput/12 | 10 | screen content: the D-scr-1 guard (encoder_ext.rs:817)
EncodeDecodeTestAPI.ScreenContent_LosslessLink0_EnableLongTermReference | 10 | screen content: the D-scr-1 guard (encoder_ext.rs:817)
EncoderInitTest.ScreenContentScrollMotionVectorBounds | 10 | screen content: the D-scr-1 guard (encoder_ext.rs:817)

# --- HashFunctions.h:20 — the POC tiebreak ----------------------------------
# Deliberate divergence, argued at decoder_conformance_test.rs:238 and corroborated
# by the JVT gold for CABA2_SVA_B, which upstream itself fails. D-poc-1 is the
# user's call: keep (JVT-correct) or match upstream (bug-compatible drop-in).
DecodeFile/DecoderOutputTest.CompareOutput/39 | D-poc-1 | decided 2026-08-22: the port keeps the JVT-correct POC tiebreak (decoder_conformance_test.rs:238) — permanent row, by design
