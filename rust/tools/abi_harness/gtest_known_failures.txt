# The `test/api` rows the Rust cdylib is known to fail — the allowlist behind
# `gtest_stretch.sh --check` (Phase 8b session A, T8b.A1).
#
#   format:  <gtest full test name> | <owner> | <reason>
#   blank lines and `#` comments ignored; the name must be the *full* name gtest
#   prints, parametrized instances spelled out one per row (`.../0`, `/1`, `/2`).
#
# `--check` fails when a failing test is NOT here (a regression, or a new failure
# nobody owns) and when a listed test PASSES (a stale row — a lie about coverage).
# The tally therefore moves only by editing this file in the same commit as the fix.
#
# Owners are the phase/session that will make the row pass. Rows are grouped by the
# *assertion site* they fail at (S47: a fixture name is a surface), which is stated
# in each group's header and re-measured by `--check`'s own by-site tally.

# --- encode_options_test.cpp:556/656/705/772/844/911 + decode_api_test.cpp:666
# --- listing strategies ------------------------------------------------------
# `CreateParametersetStrategy` returns None for the three listing modes and
# `InitializeExt` refuses (the S48 shape, already right); ~310 C++ lines to port.
#
# **F89, T8b.B1 — the seventh row is `DecodeCrashTestAPI.DecoderCrashTest`, and it
# was invisible because it only fails three runs in seven.** That test opens with
# `switch (rand() % 7)` (`decode_api_test.cpp:615-633`) and takes a *listing*
# strategy on 2, 3 and 6; on the other four draws it takes `CONSTANT_ID` or
# `INCREASING_ID` and passes. Nothing pinned `rand()` before T8b.B1, so which of
# the two it was changed every run and the row was never allowlisted — the brief's
# 165/199 was an unseeded sample that happened to draw a non-listing strategy.
# With `SEED=20260822` the draw is 3 = `SPS_LISTING_AND_PPS_INCREASING`, the
# refusal fires at the test's first `InitializeExt` (0 ms), and the honest tally
# is 164/199. All seven rows leave together when 8b.B ports the strategies.
EncodeDecodeTestAPI.ParameterSetStrategy_SPS_LISTING_AND_PPS_INCREASING1 | 8b.B | SPS_LISTING_AND_PPS_INCREASING strategy not ported; InitializeExt refuses
EncodeDecodeTestAPI.ParameterSetStrategy_SPS_LISTING_AND_PPS_INCREASING2 | 8b.B | SPS_LISTING_AND_PPS_INCREASING strategy not ported; InitializeExt refuses
EncodeDecodeTestAPI.ParameterSetStrategy_SPS_LISTING_AND_PPS_INCREASING3 | 8b.B | SPS_LISTING_AND_PPS_INCREASING strategy not ported; InitializeExt refuses
EncodeDecodeTestAPI.ParameterSetStrategy_SPS_PPS_LISTING1 | 8b.B | SPS_PPS_LISTING strategy not ported; InitializeExt refuses
EncodeDecodeTestAPI.ParameterSetStrategy_SPS_PPS_LISTING2 | 8b.B | SPS_PPS_LISTING strategy not ported; InitializeExt refuses
EncodeDecodeTestAPI.ParameterSetStrategy_SPS_PPS_LISTING3 | 8b.B | SPS_PPS_LISTING strategy not ported; InitializeExt refuses
DecodeCrashTestAPI.DecoderCrashTest | 8b.B | F89: rand()%7 picks a listing strategy on 3 of 7 draws; at the pinned seed it picks SPS_LISTING_AND_PPS_INCREASING and InitializeExt refuses

# --- decode_api_test.cpp:888 — parse-only, what is left of it ----------------
# **T8b.B2 ported `DecodeParser` and its two siblings left.** `SpecSliceLoss` and
# `SpecStatistics` pass and their rows are gone; what remains is `ParseOnly_General`,
# and it no longer fails for a parse-only reason at all — its encoder asks for three
# spatial layers and `InitializeExt` refuses under S48 (T8b.A5), which is 8b.C's
# downsample. The parse-only machinery behind it is in place: the referee is
# `tests/decoder_parseonly_parity_test.rs` against `ecref --parse-only` goldens, and
# `abi_harness parseonly` runs the same rows through the dylib.
DecodeParseAPI.ParseOnly_General | 8b.C | 3 spatial layers: InitializeExt refuses at decode_api_test.cpp:888 (S48, T8b.A5). No longer a parse-only row — DecodeParser is ported (T8b.B2)

# --- BaseEncoderTest.cpp:92 and friends — denoise / downsample REFUSE at init
# --- (S48, T8b.A5) ----------------------------------------------------------
# `METHOD_DENOISE` and `METHOD_DOWNSAMPLE` are untranslated
# (`processing/mod.rs:29-39`). Until T8b.A5 asking for either **succeeded** and
# produced wrong output: `BilateralDenoising` is an empty body, and
# `DownsamplePadding` returns `RET_NOTSUPPORTED` while both callers drop it, so a
# lower spatial layer is encoded from whatever the picture pool last held. S48 says
# a feature this phase cannot finish refuses at the entry point, so
# `ParamValidationExt` now returns `ENC_RETURN_UNSUPPORTED_PARA` and `InitializeExt`
# answers `cmInitParaError`.
#
# **That cost 14 rows that were passing**, and they were passing by not looking:
# every one asserts on API behaviour (codes, frame counts, layer ids) and none on
# bytes, so the un-downsampled lower layers went unnoticed. `EncoderOutputTest/5`
# and `/7` are the two that *do* hash, and they failed before and after. All 17 of
# these rows come back together when session C ports the two plugins.
#
# The tally before this refusal was **179/199** (T8b.A4); it is the honest number
# for "how much of the reference does the port implement", and 165/199 is the
# honest number for "how much of the reference does the port implement *without
# lying about it*". The charter's S48 chooses the second.
EncodeFile/EncoderOutputTest.CompareOutput/4 | 8b.C | denoise refuses at init (S48, T8b.A5); port METHOD_DENOISE
EncodeFile/EncoderOutputTest.CompareOutput/5 | 8b.C | downsample refuses at init (S48, T8b.A5); 2 spatial layers
EncodeFile/EncoderOutputTest.CompareOutput/7 | 8b.C | downsample refuses at init (S48, T8b.A5); 4 spatial layers
EncodeDecodeTestAPI.SimulcastAVC | 8b.C | multi-layer: refuses at init (S48, T8b.A5) — passed before by never checking bytes
EncodeDecodeTestAPI.SimulcastAVCDiffFps | 8b.C | multi-layer: refuses at init (S48, T8b.A5) — passed before by never checking bytes
EncodeDecodeTestAPI.SimulcastAVC_SPS_PPS_LISTING | 8b.C | multi-layer: refuses at init (S48, T8b.A5) — passed before by never checking bytes
EncodeDecodeTestAPI.SimulcastSVC | 8b.C | multi-layer: refuses at init (S48, T8b.A5) — passed before by never checking bytes
EncodeDecodeTestAPI.DiffSlicingInDlayer | 8b.C | multi-layer: refuses at init (S48, T8b.A5) — passed before by never checking bytes
EncodeDecodeTestAPI.DiffSlicingInDlayerMixed | 8b.C | multi-layer: refuses at init (S48, T8b.A5) — passed before by never checking bytes
EncodeDecodeTestAPI.AVCSVCExtensionCheck | 8b.C | multi-layer: refuses at init (S48, T8b.A5) — passed before by never checking bytes
EncodeDecodeTestAPI.SetOptionEncParamExt | 8b.C | multi-layer: refuses at init (S48, T8b.A5) — passed before by never checking bytes
EncodeDecodeTestAPI.SetOptionEncParamBase | 8b.C | multi-layer: refuses at init (S48, T8b.A5) — passed before by never checking bytes
EncodeDecodeTestAPI.Engine_SVC_Switch_I | 8b.C | multi-layer: refuses at init (S48, T8b.A5) — passed after T8b.A3/A4, refuses now
EncodeDecodeTestAPI.Engine_SVC_Switch_P | 8b.C | multi-layer: refuses at init (S48, T8b.A5) — passed after T8b.A3/A4, refuses now
EncodeDecodeTestAPIBase/EncodeDecodeTestAPI.GetOptionTid_SVC_L1_NOLOSS/0 | 8b.C | multi-layer: refuses at init (S48, T8b.A5) — passed after T8b.A3, refuses now
EncodeDecodeTestAPIBase/EncodeDecodeTestAPI.GetOptionTid_SVC_L1_NOLOSS/1 | 8b.C | multi-layer: refuses at init (S48, T8b.A5) — passed after T8b.A3, refuses now
EncodeDecodeTestAPIBase/EncodeDecodeTestAPI.GetOptionTid_SVC_L1_NOLOSS/2 | 8b.C | multi-layer: refuses at init (S48, T8b.A5) — passed after T8b.A3, refuses now

# --- BaseEncoderTest.cpp:92 / encode_options_test.cpp:2195 / encoder_test.cpp:323
# --- screen content ----------------------------------------------------------
EncodeFile/EncoderOutputTest.CompareOutput/8 | 10 | screen content: the D-scr-1 guard (encoder_ext.rs:817)
EncodeFile/EncoderOutputTest.CompareOutput/9 | 10 | screen content: the D-scr-1 guard (encoder_ext.rs:817)
EncodeFile/EncoderOutputTest.CompareOutput/10 | 10 | screen content: the D-scr-1 guard (encoder_ext.rs:817)
EncodeFile/EncoderOutputTest.CompareOutput/11 | 10 | screen content: the D-scr-1 guard (encoder_ext.rs:817)
EncodeFile/EncoderOutputTest.CompareOutput/12 | 10 | screen content: the D-scr-1 guard (encoder_ext.rs:817)
EncodeDecodeTestAPI.ScreenContent_LosslessLink0_EnableLongTermReference | 10 | screen content: the D-scr-1 guard (encoder_ext.rs:817)
EncoderInitTest.ScreenContentScrollMotionVectorBounds | 10 | screen content: the D-scr-1 guard (encoder_ext.rs:817)

# --- HashFunctions.h:20 — the POC tiebreak ----------------------------------
# Deliberate divergence, argued at decoder_conformance_test.rs:238 and corroborated
# by the JVT gold for CABA2_SVA_B, which upstream itself fails. D-poc-1 is the
# user's call: keep (JVT-correct) or match upstream (bug-compatible drop-in).
DecodeFile/DecoderOutputTest.CompareOutput/39 | D-poc-1 | decided 2026-08-22: the port keeps the JVT-correct POC tiebreak (decoder_conformance_test.rs:238) — permanent row, by design
