# S61 (F140): the previous session close's Miri wall-time, in seconds. gates.sh
# reads the first non-comment line's first field, prints this run's time beside
# it, and WARNs past 1.3x. A session close replaces the data line with its own
# number; mid-session runs read, never write.
#
# REGIME NOTE (D-gate-6, 2026-08-24): from session E on, the session level's
# number is the parallel Miri lane's wall (one compile + four concurrent
# shards, small probe drives) — not the serial un-sharded step the 1012 below
# was measured on. The first ratio across that boundary compares two different
# instruments and the close report must say so; after E's close the series is
# lane-vs-lane and the 1.3x tripwire means what S61 intends.
#
# THE FORK PAIR IS A SECOND REGIME, AND IT HAS TWO FORMS (F168, session H).
# The two `fork_join_encodes` probes are not in the lane above; they run at
# `full`/`exit` and by explicit command. Their wall depends on **how the two
# per-probe invocations are scheduled relative to each other**, and the two
# forms are 2x apart — so a number recorded without its form is unreadable:
#
#   parallel pair (two invocations, separate CARGO_TARGET_DIRs, concurrent)
#       T9.E8  3356 s / 3449 s   pair wall ~57 min   (first green)
#       T9.E2  3294.86 / 3343.20 pair wall ~56 min
#       T9.H   3411.72 / 3493.33 pair wall 3499 s = 58 min   H's close, GREEN
#   serial pair (one probe at a time)
#       T9.H   3194.20 / 3260.15 pair wall 6463 s = 108 min   H's step 0a, GREEN
#
# Session H ran BOTH forms on the same two probes, which is what makes this
# table readable: 58 min parallel against 108 min serial. And parallel-vs-parallel
# across the context flip — H 3411/3493 against E8 3356/3449, ratio 1.017/1.013 —
# says the flip is Miri-cost-neutral on the fork, the same result E2 measured for
# the slice flip. Compare like with like: H's own serial numbers are 6-7% BELOW
# its parallel ones per probe, which is the contention, not a regression.
#
# Serial buys 3-5% per probe and costs 2x on the pair: Miri is a single-threaded
# interpreter, so two probes are two cores and they do not contend. **Run the
# pair parallel.** H's brief instructed serial on the reading that E8's numbers
# were serial; E8's own log says "~57 minutes as a parallel pair" and 3356+3449
# is 113 minutes, so they cannot have been. Do not compare a serial number to a
# parallel one and read a regression.
#
# REGIME ADDITION (D-fid-3, 2026-08-26, session X2): the session battery gained a
# filtered gtest smoke (`EncodeDecodeTestAPI.Simulcast*`, ~9 s, self-budgeted at
# 60 s). It is in the **native lane**, deliberately, so the number below stays
# lane-for-lane comparable with every close since session E — the smoke does not
# touch it. What it does touch is the level's TOTAL wall, which D-gate-6 caps at
# 15 minutes, and X2's close measured **977 s = 16.3 min — over the cap**. The
# smoke is not the cause: 977 - 9 = 968 s is still over, so the battery was
# already past 900 s before it joined. See F185.
#
# REGIME NOTE (session H2, 2026-08-26): D-gate-6 is amended and the cap is
# **1200 s**, not 900 — the line above was written before the amendment and its
# "over the cap" verdict is against the old number. H2's battery ran **551 s**,
# less than half the cap, and that is NOT a coverage change: the architecture is
# X2's exactly. It is machine load, which is F170's caveat, and it is why S61
# compares lane against lane and not battery against battery.
#
# H2 also priced the tax D-gate-6 predicted without pricing: the debug sweep ran
# **63 s inside the session battery against 47 s in a family battery on the same
# tree** — 1.34x, the Miri lane taxing the native one.
#
# H2's fork pair shares one CARGO_TARGET_DIR rather than two, which the note above
# prescribes. The build was already warm from the lane's compile, so the two runs
# were concurrent from the start; the deviation is recorded because a pair wall is
# unreadable without its form.
# REGIME NOTE (F170, session J, 2026-08-27): the gates time with wall clock,
# so a suspended or loaded machine reads as a codec regression — one session
# lost time to a 125x phantom, and H2 measured the Miri lane taxing the native
# lane's own sweep 1.34x. From J on, `gates.sh` measures the *children CPU*
# (user+sys, via the shell's `times` builtin) beside wall for the sweeps and
# the Miri lane, and S61's 1.3x tripwire compares CPU against CPU whenever the
# data line below carries a `cpu=<seconds>` column. CPU is load- and
# suspend-immune, which is the whole point; wall stays printed beside it
# because it is what a human waits through. The first close after this note has
# a wall-only baseline: its report compares wall-vs-wall one last time and
# seeds the cpu column. NOTE ALSO: the phase ends at J, and with it the
# session-lane series — J's own close runs the `exit` battery (serial Miri,
# full probe drives), which is not lane-comparable; the lane numbers stand as
# the phase's record.
#
# Format: <seconds> <commit> <date> <note> [cpu=<seconds>]
# REGIME NOTE (safe-conversion S1, 2026-08-27): the phase-9 session-lane series
# resumes here — the safe-conversion plan's sessions close at the same `session`
# level, on the same four-shard lane, so S1's number is lane-comparable with
# H3's. **This close seeds the cpu column**, which F170 asked the first close
# after its note to do; from S2 on the tripwire compares CPU against CPU.
541 1b3471c4 2026-08-28 safeplan-S5-close-295-0-cpu-ratio-1.00-vs-1147 cpu=1151
# GAP NOTE (S8, 2026-08-30): the line above is **S5's** close, not S7's. S6 and
# S7 both closed at the `session` level and neither advanced this file, so the
# ratio S8's runs printed (1.05, then 1.06) is measured against a baseline three
# sessions old — it is a real comparison, but it spans S6's layer flip and S7's
# context flip as well as S8's own work, and no single session owns the +6%.
# The tripwire is 1.3x and this is nowhere near it; the point of the note is that
# the next close should compare against S8 and get a one-session delta again.
562 7af6689d 2026-08-30 safeplan-S8-close-296-0-cpu-ratio-1.06-vs-1151-baseline-3-sessions-stale cpu=1224
